using UnityEngine;

public class logpratice : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        int i = 3;
        int j = 5;
        bool r = false;
         
        
       
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
