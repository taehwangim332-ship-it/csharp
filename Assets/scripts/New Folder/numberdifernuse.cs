using UnityEngine;

public class numberdifernuse : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
        /*
         두개의 정수를 입력받아 두 정수의 차이를 양의 정수로 구하세요 
        first,second

        [output]
        {first}과{second}의 차이는 {diff}이다 



        */




    }

    // Update is called once per frame
    
    
            // Unity Inspector에서 값을 직접 입력받을 수 있도록 public 변수로 선언
    public int first = 10;
    public int second = 3;
    /*
      void Start()
    {
  
        int diff = Mathf.Abs(first - second);

        Debug.Log(first + "과" + second + "의 차이는 " + diff + "이다");


        //두수의 차리 출력 

        Debug.Log($"{first}보다 세컨드보다 클때를 말하는거다 ");
    */

    }


